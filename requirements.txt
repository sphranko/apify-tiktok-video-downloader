apify~=2.0
apify-client~=1.8
playwright~=1.44
httpx~=0.27
# Workaround: pydantic>=2.10 causes "cannot specify both default and default_factory"
# when crawlee is imported via apify, so pin to <2.10 until crawlee is fixed.
pydantic<2.10
